export const AUDIO_ANALYSER_FFT_SIZE = 4096;

/** Interval between render-loop FPS samples, in milliseconds. */
export const FPS_SAMPLE_INTERVAL_MS = 500;

/** Width of the visible time window for the Waveform Overview visualization, in seconds. */
export const WAVEFORM_OVERVIEW_WINDOW_SEC = 16;
